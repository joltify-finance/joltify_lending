syntax = "proto3";
package joltify.third_party.dydxprotocol.delaymsg;

option go_package = "github.com/joltify-finance/joltify_lending/x/third_party_dydx/delaymsg/types";

// BlockMessageIds stores the id of each message that should be processed at a
// given block height.
message BlockMessageIds {
  // ids stores a list of DelayedMessage ids that should be processed at a given
  // block height.
  repeated uint32 ids = 1;
}
